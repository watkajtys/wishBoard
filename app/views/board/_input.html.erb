<%= form_for(@post) do |f| %>
   <div class ="hidden">
      <%= f.hidden_field :prepend, :value => "I wish" %>
   </div>
   <div class="field">
      <%= f.label :entry %><br />
      <%= f.text_field :entry %>
   </div>
   <div class="actions">
      <%= f.submit "Write" %>
   </div>
<% end %>